name: Testing

on:
  [push]

jobs:
  thanks:
    runs-on: macOS-latest
    steps:
      - uses: actions/checkout@v2
      
      - run: 'BudgetKeeper/kobiton/xcuitest.js'

      - name: install
      - run: npm install
      
      - name: test
      - run: node xcuitest.js
